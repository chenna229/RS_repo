-- Replace RETURN Result with this to debug
RETURN
SWITCH(
    TRUE(),
    A = 0, "A is blank - prev period not found",
    B = 0, "B is 0 - no carried over errors found",
    C = 0, "C is 0 - denominator blank",
    Result
)
